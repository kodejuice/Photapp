import React from 'react';
import Header from './Header';

const Profile: React.FC<{}> = ()=>{
    return (
        <Header page='profile'>
            hello
        </Header>
    );
};

export default Profile;
